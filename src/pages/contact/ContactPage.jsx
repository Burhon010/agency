import PagePlaceholder from '@/components/common/PagePlaceholder'

/** /contact — Contact us (Figma: Contact us). */
export default function ContactPage() {
  return <PagePlaceholder title="Contact Us" sections={['Форма: имя, email, сообщение + кнопка отправки', 'Промо-баннер']} />
}
